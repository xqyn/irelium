# .readthedocs.yaml
version: 0.1.0

build:
  os: macOS
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs-sphinx